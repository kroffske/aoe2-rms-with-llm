---
description: /reflection — Analyze work process, propose minimal improvements. (project)
argument-hint: [--push [repo]] <chat context | tmp.txt path | problem description>
---

# /reflection — Анализ работы

> 💭 **Reflection**: анализирую процесс, предлагаю минимальные улучшения

## Цель

Осмотреть процесс выполнения задачи и предложить **точечные** улучшения в скиллах/AGENTS.md.

## Input

<context>
$ARGUMENTS
</context>

## Режимы работы

| Режим | `<context>` содержит | Что делать |
|-------|---------------------|------------|
| **Chat Analysis** | пусто | Анализ текущего диалога → промт-блок |
| **File Input** | путь к файлу | Read файл → использовать как контекст |
| **Direct Input** | текст | Контекст уже есть, начинай анализ |
| **--push** | `--push` или `--push <repo>` | После рефлексии создать GitHub Issue |

## Ключевые принципы

1. **Минимальные правки** — НЕ переписывать файлы с нуля, только точечные изменения
2. **План → Апрув → Работа** — НИКАКИХ изменений без согласия пользователя
3. **1-3 правки максимум** — лучше меньше, но качественнее

## Контекст выполнения

Перед анализом определи, где ты находишься:

| Контекст | Признаки | Куда вносить правки |
|----------|----------|---------------------|
| **Plugin repo** | Есть `.claude-plugin/marketplace.json` | `.claude/commands/*.md`, `.claude/skills/*/SKILL.md` |
| **Local project** | Плагин установлен через `/plugin install` | Только `.claude/` (локальные оверрайды) или создать issue/PR в source repo |

**Важно**: Если в local project — НЕ редактируй файлы плагина напрямую (они перезапишутся при обновлении). Предложи:
1. Локальный оверрайд в `.claude/commands/` или `.claude/rules/`
2. PR в репо плагина (если улучшение универсальное)

## Процедура

### Phase 0: DETECT MODE + REPO

**Think step by step. Use ultrathink for complex analysis.**

#### 0.1 Определи режим

Посмотри на `<context>` выше:

| Содержимое `<context>` | Режим | Действие |
|------------------------|-------|----------|
| Пусто / пробелы | Chat Analysis | Анализируй текущий диалог |
| Путь к файлу (`*.txt`, `*.md`, содержит `/`) | File Input | `Read` файл, использовать как контекст |
| Текст / описание проблемы | Direct Input | Контекст уже в `<context>` |

#### 0.2 Определи репозиторий

| Признак | Репо | Куда правки |
|---------|------|-------------|
| `.claude-plugin/marketplace.json` | Plugin repo | `.claude/commands/`, `.claude/skills/` |
| `CLAUDE.md` с "locus-claude-kit" | Plugin repo | Прямые правки |
| Только `.claude/` без plugin | Local project | Только `.claude/` оверрайды |

**Если Local Project:** НЕ редактируй файлы плагина. Предложи:
1. Оверрайд в `.claude/commands/` или `.claude/rules/`
2. Issue/PR в source repo

---

### Phase 1: UNDERSTAND (если Context Input)

**Перед анализом — пойми проблему.**

```markdown
## 📋 Как я понял проблему:

**Источник:** {chat / file path / direct input}
**Проблема:** {1-2 предложения}
**Симптомы:** {что пошло не так}
**Гипотеза:** {почему это произошло}

---
Это верно?
```

→ Дождись подтверждения перед Phase 2.

---

### Phase 2: Анализ (без изменений файлов!)

**Think step by step. Analyze systematically.**

#### 2.1 Сбор контекста

- История диалога, план, изменения
- Результаты тестов/линта
- Фидбек пользователя

#### 2.2 Multi-Agent оценка (опционально)

**Для сложных проблем** запусти 1-4 агентов параллельно:

| Агент | Когда использовать | Фокус |
|-------|-------------------|-------|
| `scout-haiku` | Нужен контекст кодовой базы | Найти связанные файлы |
| `expert` | Оценка целесообразности | DO / DON'T / PARTIAL |
| `architect` | Архитектурные изменения | Паттерны, структура |
| `reviewer-opus` | Качество кода | AGENTS.md compliance |

```markdown
**Prompt для агентов:**

Контекст: {problem description}
Репозиторий: locus-claude-kit (Claude Code plugin toolkit)
Задача: Оцени предлагаемые изменения.

Think step by step. Use ultrathink for deep analysis.

Вернуть:
- Status: ✅ APPROVE / ⚠️ CONCERNS / ❌ REJECT
- Обоснование (2-3 предложения)
- Конкретные предложения
```

#### 2.3 Анализ причин

- Почему отклонения возникли?
- Что вызвало сложность/задержки?
- Какие инструкции были неясны/отсутствовали?

### Phase 3: План изменений (показать пользователю!)

Сформировать и ПОКАЗАТЬ пользователю:

```markdown
## Reflection Plan: [Название задачи]

### Что сработало
- ...

### Что можно лучше
- ...

### Предлагаемые изменения (1-3 max)

1. **Файл**: `path/to/file.md`
   **Что**: добавить правило X в секцию Y
   **Почему**: предотвратит проблему Z

2. ...

---
**Ждём апрув для внесения изменений.**
```

⚠️ **СТОП! Дождаться ответа пользователя.**

---

### Phase 3.1: Output (Chat Analysis режим)

**Если анализировали чат для другого проекта** — вернуть промт-блок для копирования:

````markdown
## 📋 Reflection Prompt

Скопируй этот блок в целевой репозиторий:

```
/reflection

## Проблема
{описание проблемы из анализа}

## Симптомы
{что пошло не так}

## Предлагаемые изменения
1. {изменение 1}
2. {изменение 2}

## Контекст
{краткий контекст из чата}
```
````

---

### Phase 4: Применение (только после апрува!)

- Вносить ТОЛЬКО одобренные изменения
- Использовать Edit tool для точечных правок
- НЕ переписывать файлы целиком через Write

## Формат правок

```python
# BAD: переписать весь файл
Write(file_path, new_content)

# GOOD: точечная правка
Edit(file_path, old_string="...", new_string="...")
```

## Ограничения

- **Max 3 файла** за одну рефлексию
- **Max 20 строк** изменений на файл
- Если нужно больше → создать таск в `tasks/backlog/`

## Чек-лист

- [ ] Показал план пользователю?
- [ ] Дождался апрува?
- [ ] Правки минимальны (не переписывание)?
- [ ] Не более 3 файлов затронуто?
- [ ] Каждая правка обоснована?

---

## --push: Публикация результатов

**Если в аргументах есть `--push`** — после завершения рефлексии создать GitHub Issue.

### Синтаксис

```
/reflection --push <описание проблемы>
/reflection --push kroffske/other-repo <описание проблемы>
```

### Логика

1. **Парсинг аргументов:**
   - `--push` без repo → использовать `kroffske/locus-claude-kit`
   - `--push owner/repo` → использовать указанный repo
   - Остаток аргументов → контекст для анализа

2. **После Phase 3 (план изменений)** выполнить:

```bash
gh issue create \
  --repo <repo> \
  --title "[Reflection] <краткое описание проблемы>" \
  --body "<содержимое плана изменений из Phase 3>" \
  --label "reflection,feedback"
```

3. **Вывести ссылку** на созданный issue.

### Пример

```
/reflection --push Агент не следует handoff протоколу
```

Создаст issue в `kroffske/locus-claude-kit` с результатами рефлексии.
