---
description: Implement task from epic. Triggers on "реализуй задачу", "выполни таск", "implement task", "do the task", "work on task".
argument-hint: <epic-slug> [task-id]
---

# Task Implementation

> Атомарное выполнение задачи из epic. Используй для работы по существующему плану.

**Epic**: `$ARGUMENTS`

---

## Step 1: Load Context

### 1.1 Найти epic

```
Искать в (приоритет):
1. tasks/{epic-slug}/epic.md          <- epic-as-folder (preferred)
2. tasks/YYYY-MM-DD-{epic-slug}/epic.md
3. tasks/{epic-slug}.md               <- legacy format
```

### 1.2 Прочитать epic

Read `epic.md` и извлечь:
- Goal и Context
- Tasks Overview с checkbox статусом
- Artifacts (если есть `artifacts/plan.md`)
- Связанные файлы и модули

### 1.3 Найти задачу для выполнения

**Если task-id указан**:
- Найти задачу по ID/номеру

**Если task-id НЕ указан**:
- Взять первую незавершённую задачу `[ ]`

### 1.4 Загрузить контекст задачи

- Read файлы которые будут затронуты
- Понять текущее состояние кода
- Проверить связанные тесты

---

## Step 2: Execute Task

### 2.1 Инициализация

```
Mark task `in_progress` в TodoWrite:
- Task: {task description}
```

### 2.2 Gather Context

```
Read существующий код:
- Файлы для изменения
- Связанные модули
- Тесты если есть
```

### 2.3 Implement Changes

Следовать правилам AGENTS.md:
- Single responsibility
- Fail fast, no fallbacks
- Explicit dependencies
- Pydantic over dataclass
- No isinstance-branching

### 2.4 Verify

```bash
# Проверить изменённые файлы
ruff check {changed_files}

# Запустить тесты модуля
pytest tests/unit/{module}/ -v --tb=short

# Если integration тесты
pytest tests/integration/{module}/ -v --tb=short
```

### 2.5 Accept/Reject

**Если verify прошёл ✅**:
- Продолжить к Step 3

**Если verify упал ❌**:
- Исправить ошибки
- Повторить 2.4

---

## Step 3: Update Epic

### 3.1 Отметить задачу выполненной

Edit epic файл:
```markdown
# До
- [ ] Task description

# После
- [x] Task description
```

### 3.2 Обновить artifacts (если epic-as-folder)

Если epic — папка и созданы значимые артефакты:
```bash
# Добавить заметки в artifacts/
echo "## Implementation Notes" >> tasks/{epic}/artifacts/implementation.md
```

### 3.3 Update TodoWrite

```
Mark task `completed` в TodoWrite
```

---

## Step 4: Commit

### 4.1 Определить тип коммита

| Изменение | Тип |
|-----------|-----|
| Новая функциональность | feat |
| Исправление бага | fix |
| Рефакторинг | refactor |
| Тесты | test |
| Документация | docs |
| Инфраструктура | chore |

### 4.2 Определить scope

Scope = модуль/компонент:
- `cli` - CLI команды
- `retrieval` - Retrieval pipeline
- `ingestion` - Ingestion pipeline
- `api` - FastAPI сервис
- `config` - Конфигурация

### 4.3 Создать коммит

```bash
git add {changed_files}
git commit -m "$(cat <<'EOF'
{type}({scope}): {description}

{body - опционально, если нужны детали}

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
EOF
)"
```

---

## Step 5: Next Task

### 5.1 Проверить есть ли следующая задача

Read epic файл, найти следующую `[ ]` задачу.

### 5.2 Предложить продолжение

**Если есть незавершённые задачи**:
```
Задача выполнена ✅

Следующая задача: {next task description}
Продолжить? (можно запустить /implement {epic} снова)
```

**Если epic завершён**:
```
Epic завершён! ✅

Все задачи выполнены.
Рекомендуется:
1. /lint - проверить качество
2. /push - создать релиз
```

---

## Правила

1. **ОДНА задача за раз** - атомарное выполнение
2. **Verify обязателен** - никогда не пропускать
3. **Коммит на каждую задачу** - трекаемая история
4. **Обновлять epic** - отмечать [x] сразу
5. **Следовать AGENTS.md** - это закон проекта
